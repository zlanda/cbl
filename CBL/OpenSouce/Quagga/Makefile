################################################################################
all:
	@echo "Make quagga begin."
	#执行脚本方法，只要在主Makefile中指定脚本执行路径，即可在主Makefile中
	#执行脚本编译，非常方便，更主要的是非常简单
	./Build.sh -b
################################################################################

#清除编译内容
clean:
	#echo前加上@能在Makefile中执行并回显，不加@能执行但不回显
	@echo "Clean quagga."
	#执行清除脚本方法
	./Build.sh -c
	
